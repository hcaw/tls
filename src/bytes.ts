// TODO: Open source a starter repo with all of the config setup!
export const writeBytes = (bytes: Uint8Array, data: Uint8Array): Uint8Array => {

};

const startLength = (bytes: Uint8Array, length: number, ) => () => {}
